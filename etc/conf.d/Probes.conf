{
    'cpu'             => do "$ENV{AKKADA}/etc/conf.d/Probes/cpu.conf",
    'dns_query'       => do "$ENV{AKKADA}/etc/conf.d/Probes/dns_query.conf",
    'dns_server'      => do "$ENV{AKKADA}/etc/conf.d/Probes/dns_server.conf",
    'group'           => do "$ENV{AKKADA}/etc/conf.d/Probes/group.conf",
    'hdd'             => do "$ENV{AKKADA}/etc/conf.d/Probes/hdd.conf",
    'nic'             => do "$ENV{AKKADA}/etc/conf.d/Probes/nic.conf",
    'node'            => do "$ENV{AKKADA}/etc/conf.d/Probes/node.conf",
    'ntp'             => do "$ENV{AKKADA}/etc/conf.d/Probes/ntp.conf",
    'snmp_generic'    => do "$ENV{AKKADA}/etc/conf.d/Probes/snmp_generic.conf",
    'softax_ima'      => do "$ENV{AKKADA}/etc/conf.d/Probes/softax_ima.conf",
    'softax_ping'     => do "$ENV{AKKADA}/etc/conf.d/Probes/softax_ping.conf",
    'ssl_generic'     => do "$ENV{AKKADA}/etc/conf.d/Probes/ssl_generic.conf",
    'tcp_generic'     => do "$ENV{AKKADA}/etc/conf.d/Probes/tcp_generic.conf",
    'windows_service' => do "$ENV{AKKADA}/etc/conf.d/Probes/windows_service.conf",
}
